<!DOCTYPE html>
<html lang="en">

<head>
  <title>Home Page</title>

  <link rel="stylesheet" href="/vendor/normalize-4.1.1.css" type="text/css" />
  <link rel="stylesheet" href="/vendor/border-box.css" type="text/css" />
  <link href="/styles/home.css" rel="stylesheet" type="text/css" >

  <script type="text/javascript" src="/vendor/jquery-3.0.0.js"></script>
  <script type="text/javascript" src="/scripts/app.js"></script>

</head>
<%- include('./partials/_header') %>
<body>
  <h1></h1>
  <div class="wrap">
    <div class="search">
      <form class="search-bar" method="POST" action="/search">
        <input type="text" placeholder="Search" name="search">
        <button type="submit"><i class="fa fa-search"></i></button>
      </form>
    </div>
    <!-- <div class="filter">
        <form method="GET" action='/mostrecent'><button class="submit">Most Recent</button>
          make into drop down list for filter -->
     </div> -->
     <div class="myresources">
        <form method="GET" action='/myresources'><button class="submit">My Resources</button>
          <!-- make into drop down list for filter -->
     </div>
     <div class="profile">
        <form method="GET" action='/profile'><button class="submit">My Profile</button>
          <!-- make into drop down list for filter -->
     </div>
  </div>
<section id="resource-container">
  <% for (let i = 0; i < resource.data.length; i++) { %>
    <div class="resource-box">
         <span class="resource-title"><%= resource.data[i].title %></span>
         <img class="resource-img" src=<%= resource.data[i].image_url %> />
         <% if (resource.userId) { %>
         <form method="POST" action="/like/<%=resource.data[i].id%>?_method=PUT"><button class="submit">Like</button></form>
         <% } else { %>
        <form method="GET" action="/login"><button class="submit">Like</button></form>
        <% } %>
    </div>
  <% } %>
</section>
</body>

</html>
